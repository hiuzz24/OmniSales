package fu.osms.orderreturn.enums;

public enum OrderReturnStatus {
    PENDING_APPROVAL,
    REJECTED,
    AWAITING_RETURN,
    RETURN_IN_TRANSIT,
    INSPECTED,
    PLATFORM_PROCESSING,
    PENDING_STOCK,
    COMPLETED,
    FAILED
}
